# Dynamic-Array
A dynamic array class in C++ with zero memory leak.

This project built a dynamic array class, which can grow as necessary as new elements are added to the array. Futher, the memory re-allocation and deleting is in-place. The available APIs are specified in the array.h file.
